 <div class="container">
    <h1>Posts by {{ profile }}</h1>
    <div class="homeList">
        {{# each posts }}
            <a href="/posts/{{ this._id }}"+>{{ this.title }}</a>
            <br>
            <div class="divider"></div>
        {{/each}}
    </div>

</div>
