 <div class="container">
    <h1>Posts by {{ profile }}</h1>
    <div class="homeList">
        {{# each posts }}
            <div class="ml-5">
                <a href="/posts/{{ this._id }}">{{ this.title }}</a>
                <br>
            </div>
            <div class="divider"></div>
        {{/each}}
    </div>

</div>
