 <div class="container">
    <h1>Profile Page</h1>
    <div class="homeList">
        {{# each posts }}
            <a href="/posts/{{ this._id }}"+>{{ this.title }}</a>
            <br>
            <div class="divider"></div>
        {{/each}}
    </div>

</div>
